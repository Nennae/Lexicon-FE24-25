
function throwDices() {
      let knockOutNums = [6, 7, 8, 9];
      let targetEndScore = 12;
      let playerScore = 0;
      let playerThrowResults = [];

      while (playerThrowResults.length < 3) {
            let generateDiceNum = Math.floor(Math.random() * 6 + 1);
            playerThrowResults.push(generateDiceNum);

            let max = playerThrowResults.reduce((num))
      }
}

throwDices();